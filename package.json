{
  "name": "malik-project",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm ci --prefix backend --omit=dev",
    "postinstall": "npm install --prefix backend --omit=dev",
    "start": "node backend/server.js",
    "collector": "node collector.js"
  },
  "dependencies": {
    "mqtt": "^5.15.1"
  }
}
