# Service Railway à la racine du dépôt (voir nixpacks.toml)
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node backend/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
